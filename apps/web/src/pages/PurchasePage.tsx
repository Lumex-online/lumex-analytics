import { DashboardPage } from "./DashboardPage";

export function PurchasePage() {
  return (
    <DashboardPage
      dashboardKey="purchase"
      title="Purchase Dashboard"
      description="Purchase value, warehouse-level inbound activity, and procurement visibility inside the current analytics scope."
    />
  );
}
