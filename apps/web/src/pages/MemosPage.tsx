import { DashboardPage } from "./DashboardPage";

export function MemosPage() {
  return (
    <DashboardPage
      dashboardKey="memos"
      title="Memo Analytics"
      description="Memo given, converted memo value, open memo exposure, and buyer-level conversion behavior with backend-enforced scope."
    />
  );
}
